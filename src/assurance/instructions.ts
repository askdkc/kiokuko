export const MEMORY_ASSURANCE_INSTRUCTIONS = 'When available, inspect task_memory_status for the current delivery and assurance revision. Use task_memory_review for applicable delivered memories: adopt with current basis, invariant, counterexample and verifier, or explain inapplicability/contradiction. Reading alone is not application. For code changes link passing execution evidence to adopted memories; task_execution_evidence is model-reported, while Codex hook evidence is client-observed. Changed targets or deliveries invalidate prior verification. Use task_memory_refresh on the same run for new paths/errors; do not repeat task_prepare. PLAN and code review need grounded decisions, not implementation test runs. Preserve failure/interruption/cancellation as valid terminal outcomes. Capture corrections through memory_capture with exact entry ID/revision, condition, invariant, counterexample and verification; never auto-promote a declaration. Unsupported clients retain advisory operation and must not claim hook observation.';
