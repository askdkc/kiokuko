import { STANDARD_SOUL_SKILL_NAME, STANDARD_SIMPLE_SKILL_NAME, STANDARD_FUNCTION_SKILL_NAME, STANDARD_UI_SKILL_NAME } from '../setup/standard-skills.js';

/** Shared intake and Skill-routing instructions for MCP and generated agent files. */
export const SOUL_ROUTING_ENTRY_CONTRACT = `Before any non-trivial Kiokuko-governed work, read and apply the complete bundled \`${STANDARD_SOUL_SKILL_NAME}\` Skill before any other Kiokuko Skill. Every \`task_prepare\` call must set \`soulRead: true\` only after that read; omission or false is invalid. This is an explicit client attestation, not remote proof of model cognition. The exact local \`${STANDARD_SOUL_SKILL_NAME}\` capability is required for every task and missing or unknown availability fails closed. Akinator is the mandatory intake state machine before every planning or implementation route: call \`task_prepare\` once, resolve each exact current question through grounded \`task_answer\` calls, and do not plan, implement, verify, enter simple/code/UI routes, or checkpoint while \`intake.status=needs_answer\` or \`nextAction=answer_from_evidence_or_ask_user\`. Route only after intake reaches \`ready\` or \`exhausted\` and top-level \`nextAction\` permits progress. Follow this canonical router to \`${STANDARD_SIMPLE_SKILL_NAME}\` for bounded simple code work or an explicit minimal/YAGNI request, to \`${STANDARD_FUNCTION_SKILL_NAME}\` for all code work, and to \`${STANDARD_UI_SKILL_NAME}\` for interactive UI work. The simple-work route minimizes the solution but never replaces the code contract or waives required validation, security, accessibility, error handling, or verification. Read every applicable specialist \`SKILL.md\` index, then only the expert fragments selected by the current task or concrete risk; do not load every reference by default. Never substitute, install, or execute fetched external Skill content.`;
